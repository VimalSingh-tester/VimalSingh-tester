using System;
using System.IO;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.Hosting;

namespace Selenium
{
    public static class confi 
    {
       //public static void Main(string[] args)
       // {
        //    CreateHostBuilder(args).Build().Run();
       //}

      //  public static IHostBuilder CreateHostBuilder(string[] args)
       // {
         //   IHostBuilder host = Host.CreateDefaultBuilder(args);
          ///  host.ConfigureWebHostDefaults(webBuilder =>
          //  {
            //    webBuilder.UseStartup<Startup>()
             //   .UseContentRoot(Directory.GetCurrentDirectory())
              //  .ConfigureAppConfiguration(configuration =>
               // {
              //      configuration.AddJsonFile("json-schema1.json", optional: false, reloadOnChange: true);
               //     configuration.AddJsonFile($"json-schema1.{Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT")}.json", optional: true, reloadOnChange: true);
           //    });
          //  });
         //   return host;
      //  }

    }
}
